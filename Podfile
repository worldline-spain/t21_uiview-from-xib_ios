#sources
source 'https://github.com/CocoaPods/Specs.git'

workspace 'T21UIViewFromXIB'
project 'MyPod'

target 'MainTarget' do
    #Dependencies for the primary target (the main app or the main library)
    
    target 'Tests' do
        # Dependencies for the Testing target
        
    end
end
